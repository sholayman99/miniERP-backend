services:
  - type: web
    name: erp-backend
    env: node
    rootDirectory: .
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
